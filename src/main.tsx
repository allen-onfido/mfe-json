import React from 'react';
import ReactDOM from 'react-dom/client';
import MfeJsonApp from './mfe-json-app.tsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MfeJsonApp />
  </React.StrictMode>,
);
